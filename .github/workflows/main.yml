```yaml
name: Test GitHub Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f2000029@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Final Step
        run: echo "Workflow completed"
```
